This script adds two separates arrays to a new array moving from one array and grabbing a value and then moving to the another and grabbing a value. This continues until both arrays are exhausted of their elements. — Edit
